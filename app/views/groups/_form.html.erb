<%= form_for(@group) do |f| %>
  <%= render partial: "shared/form_errors", locals: { object: @group, object_name: :group } %>

  <%= f.field :name, as: :text %>
  <%= f.field :size, as: :number %>
  <%= f.submit_button %>
<% end %>
