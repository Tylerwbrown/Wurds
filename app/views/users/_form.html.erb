<%= form_for(@user) do |f| %>
  <%= render partial: "shared/form_errors", locals: { object: @user, object_name: :user } %>

  <%= f.field :name, as: :text %>
  <%= f.field :group, as: :text %>
  <%= f.submit_button %>
<% end %>
